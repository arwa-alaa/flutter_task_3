import 'package:untitled/quiz%20model/questions.dart';

class Quiz {
  List<Question> quiz = [
    Question("Is it a Tree ? ", true),
    Question("Is it a Sun ? ", false),
    Question("Is it a Maroon (Color) ? ", true),
  ];
}
